# UnownedCoordinator example app

This repository contains a sample app for the UnownedCoordinator concept where the memory is automatically managed by iOS runtime, cleaning up coordinators when owned views disappear.
